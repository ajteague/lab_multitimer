"""Core services for the Shock Timer Streamlit application.

The package intentionally keeps the persisted session keys stable so existing
experiments can be opened by newer versions of the application.
"""
